# 🚀 Быстрый старт Chocodealers Bot

## За 5 минут до первого запуска!

### Шаг 1: Создайте Telegram бота

1. Откройте Telegram
2. Найдите **@BotFather**
3. Отправьте `/newbot`
4. Следуйте инструкциям (имя бота, username)
5. **Скопируйте токен** - он понадобится!

### Шаг 2: Узнайте свой Telegram ID

1. Найдите **@userinfobot** в Telegram
2. Отправьте любое сообщение
3. **Скопируйте ваш ID** (число, например: 123456789)

### Шаг 3: Настройте базу данных (Вариант А - Локально)

```bash
# Установите PostgreSQL если нет
# macOS:
brew install postgresql@14
brew services start postgresql@14

# Ubuntu/Debian:
sudo apt-get install postgresql-14

# Создайте базу
createdb chocodealers

# Примените схему
psql chocodealers < database/schema.sql
psql chocodealers < database/seed_data.sql
```

### Шаг 3: Настройте базу данных (Вариант Б - Railway)

1. Зайдите на [railway.app](https://railway.app)
2. Войдите через GitHub
3. New Project → Provision PostgreSQL
4. **Скопируйте DATABASE_URL** из Variables

### Шаг 4: Настройте переменные окружения

```bash
# Скопируйте пример
cp .env.example .env

# Откройте редактор
nano .env

# Заполните МИНИМУМ:
TELEGRAM_BOT_TOKEN=ваш_токен_от_BotFather
DATABASE_URL=postgresql://...
ADMIN_TELEGRAM_IDS=ваш_telegram_id
SECRET_KEY=любая_случайная_строка
```

### Шаг 5: Установите зависимости

```bash
# Создайте виртуальное окружение
python -m venv venv
source venv/bin/activate

# Установите пакеты
pip install -r requirements.txt
```

### Шаг 6: Добавьте себя в базу как админа

```bash
# Подключитесь к базе
psql chocodealers  # или psql $DATABASE_URL

# Выполните:
INSERT INTO users (telegram_id, username, first_name, role)
VALUES (123456789, 'ваш_username', 'Ваше Имя', 'ADMIN');

# Выход
\q
```

### Шаг 7: Запустите бота!

```bash
python bot/main.py
```

Вы должны увидеть:
```
✅ Bot started successfully!
Press Ctrl+C to stop
```

### Шаг 8: Протестируйте

Откройте Telegram, найдите вашего бота и напишите:
```
/start
```

**ГОТОВО! 🎉**

---

## 📌 Минимальная конфигурация .env

Для быстрого старта достаточно этого:

```env
# Telegram
TELEGRAM_BOT_TOKEN=123456:ABCdefGHIjkl...
ADMIN_TELEGRAM_IDS=123456789

# Database
DATABASE_URL=postgresql://user:pass@localhost/chocodealers

# Безопасность
SECRET_KEY=your-random-secret-key-change-this

# Приложение
ENVIRONMENT=development
LOG_LEVEL=INFO
```

Остальные параметры (Square, Google Sheets) можно добавить позже!

---

## ❓ Что-то не работает?

### Бот не отвечает в Telegram
- Проверьте, запущен ли `python bot/main.py`
- Убедитесь, что токен правильный
- Проверьте логи: `cat logs/chocodealers_bot.log`

### Ошибка подключения к базе
- Убедитесь, что PostgreSQL запущен
- Проверьте DATABASE_URL
- Примените схему: `psql chocodealers < database/schema.sql`

### "Вы не зарегистрированы в системе"
- Добавьте себя в базу (Шаг 6)
- Убедитесь, что ваш Telegram ID правильный

---

## 🎯 Следующие шаги

После успешного запуска:

1. **Протестируйте команды:**
   - `/остатки` - проверьте инвентарь
   - `/продажа BAR-S-01 1` - попробуйте продажу
   - `/help` - изучите все команды

2. **Добавьте других пользователей:**
   ```
   /добавить_пользователя <их_telegram_id> STAFF
   ```

3. **Настройте интеграции** (опционально):
   - Square POS - для синхронизации продаж
   - Google Sheets - для отчетов
   - Google AI - для аналитики

4. **Задеплойте в продакшн:**
   - См. README.md раздел "Деплой"
   - Рекомендуем Railway.app (бесплатный план на старте)

---

Удачи! 🍫 🚀
