# 🍫 Chocodealers Warehouse Management Bot

Профессиональный Telegram-бот для управления складом, продажами и производством шоколадного магазина Chocodealers на острове Панган, Таиланд.

## 🚀 Основные возможности

- **📦 Управление инвентарем** - Отслеживание остатков товаров и ингредиентов
- **💰 Учет продаж** - Регистрация продаж через Telegram
- **🏭 Производство** - Учет производства партий шоколада
- **🛒 Закупки** - Управление закупками сырья
- **📊 Отчетность** - Автоматическая генерация отчетов
- **🔄 Интеграции**:
  - Square POS - синхронизация продаж и инвентаря
  - Google Sheets - экспорт данных для анализа
  - Google AI - AI-аналитика и инсайты
- **👥 Система ролей** - ADMIN, MANAGER, STAFF с разными правами
- **🔔 Уведомления** - Алерты о низких остатках

## 📋 Требования

- Python 3.11+
- PostgreSQL 14+
- Telegram Bot Token
- (Опционально) Square API credentials
- (Опционально) Google Sheets API credentials

## 🛠️ Установка

### 1. Клонируйте репозиторий

```bash
git clone https://github.com/yourusername/chocodealers-bot.git
cd chocodealers-bot
```

### 2. Создайте виртуальное окружение

```bash
python -m venv venv
source venv/bin/activate  # На Windows: venv\Scripts\activate
```

### 3. Установите зависимости

```bash
pip install -r requirements.txt
```

### 4. Настройте базу данных PostgreSQL

**Локальная разработка:**

```bash
# Создайте базу данных
createdb chocodealers

# Или через psql
psql -U postgres
CREATE DATABASE chocodealers;
\q
```

**Production (Railway/Heroku):**
- Railway/Heroku автоматически создадут PostgreSQL и предоставят DATABASE_URL

### 5. Инициализируйте базу данных

```bash
# Примените схему
psql -U postgres -d chocodealers -f database/schema.sql

# Загрузите начальные данные
psql -U postgres -d chocodealers -f database/seed_data.sql
```

### 6. Настройте переменные окружения

Скопируйте `.env.example` в `.env` и заполните:

```bash
cp .env.example .env
nano .env
```

**Обязательные параметры:**

```env
# Telegram Bot Token (получите у @BotFather)
TELEGRAM_BOT_TOKEN=your_bot_token_here

# PostgreSQL
DATABASE_URL=postgresql://user:password@localhost:5432/chocodealers

# Admin Telegram IDs (ваш Telegram ID)
ADMIN_TELEGRAM_IDS=123456789
```

**Опциональные параметры:**

```env
# Square API
SQUARE_ACCESS_TOKEN=your_square_token
SQUARE_LOCATION_ID=your_location_id
SQUARE_ENVIRONMENT=sandbox  # или production

# Google Sheets
GOOGLE_CREDENTIALS_FILE=./config/google_credentials.json
GOOGLE_SHEET_ID=your_sheet_id

# Google AI
GOOGLE_AI_API_KEY=your_ai_api_key
```

### 7. Найдите свой Telegram ID

Напишите боту @userinfobot в Telegram, он покажет ваш ID.

### 8. Добавьте себя как администратора

```sql
-- Подключитесь к базе
psql -U postgres -d chocodealers

-- Добавьте себя
INSERT INTO users (telegram_id, username, first_name, role)
VALUES (123456789, 'your_username', 'Your Name', 'ADMIN');
```

## 🎯 Запуск

### Локальный запуск

```bash
python bot/main.py
```

Бот должен запуститься и показать:
```
✅ Bot started successfully!
Press Ctrl+C to stop
```

### Тестирование

Напишите своему боту в Telegram:
```
/start
```

Вы должны увидеть приветственное сообщение!

## ☁️ Деплой в Production

### Railway (Рекомендуется)

1. **Создайте аккаунт на [Railway.app](https://railway.app)**

2. **Создайте новый проект:**
   - New Project → Deploy from GitHub repo
   - Выберите ваш репозиторий

3. **Добавьте PostgreSQL:**
   - В проекте: New → Database → Add PostgreSQL
   - Railway автоматически создаст DATABASE_URL

4. **Настройте переменные окружения:**
   - Variables → Add Variable
   - Добавьте все переменные из `.env`

5. **Деплой:**
   - Railway автоматически задеплоит при push в GitHub

### Heroku

```bash
# Установите Heroku CLI
heroku login

# Создайте приложение
heroku create chocodealers-bot

# Добавьте PostgreSQL
heroku addons:create heroku-postgresql:mini

# Установите переменные
heroku config:set TELEGRAM_BOT_TOKEN=your_token
heroku config:set ADMIN_TELEGRAM_IDS=123456789

# Деплой
git push heroku main

# Инициализируйте БД
heroku pg:psql < database/schema.sql
heroku pg:psql < database/seed_data.sql
```

## 📱 Использование бота

### Команды для всех пользователей

```
/start - Приветствие и помощь
/help - Список всех команд
/остатки - Показать остатки всех товаров
/остатки <SKU> - Остатки конкретного товара
/мой_профиль - Информация о профиле
```

### Команды для STAFF+

```
/продажа <SKU> <кол-во> [цена] - Зарегистрировать продажу

Примеры:
/продажа BAR-S-01 5
/продажа TRF-002 3 180
```

### Команды для MANAGER+

```
/производство <SKU> <кол-во> - Произвести товар
/закупка <код> <кол-во> - Закупить ингредиент
/отчет день - Отчет за сегодня
/отчет неделя - Отчет за неделю
/низкие_остатки - Товары с низкими остатками
```

### Команды для ADMIN

```
/синх_square - Синхронизация с Square POS
/синх_sheets - Синхронизация с Google Sheets
/пользователи - Список пользователей
/добавить_пользователя <ID> <роль> - Добавить пользователя
/изменить_роль <ID> <роль> - Изменить роль
```

## 🔧 Настройка интеграций

### Square POS

1. Создайте аккаунт на [Square Developer](https://developer.squareup.com)
2. Создайте приложение
3. Получите Access Token и Location ID
4. Добавьте в `.env`:
   ```env
   SQUARE_ACCESS_TOKEN=EAAAxxxxxx
   SQUARE_LOCATION_ID=L123456
   SQUARE_ENVIRONMENT=production
   ```

### Google Sheets

1. Создайте проект в [Google Cloud Console](https://console.cloud.google.com)
2. Включите Google Sheets API
3. Создайте Service Account
4. Скачайте JSON credentials → сохраните как `config/google_credentials.json`
5. Создайте Google Sheet и дайте доступ service account email
6. Добавьте Sheet ID в `.env`:
   ```env
   GOOGLE_SHEET_ID=1abcdefghijklmnopqrstuvwxyz
   ```

### Google AI (Notebook LM)

1. Получите API ключ на [Google AI Studio](https://makersuite.google.com)
2. Добавьте в `.env`:
   ```env
   GOOGLE_AI_API_KEY=AIzaSyXXXXXXXX
   ```

## 📊 Структура базы данных

```
users - Пользователи системы
├── products - Товары (26 SKU)
│   └── inventory_products - Остатки товаров
├── ingredients - Ингредиенты (40+)
│   └── inventory_ingredients - Остатки ингредиентов
├── sales - Продажи
├── production - Производство
│   └── production_ingredients_used - Использованные ингредиенты
└── purchases - Закупки
```

## 🐛 Решение проблем

### Бот не запускается

```bash
# Проверьте логи
tail -f logs/chocodealers_bot.log

# Проверьте подключение к БД
psql $DATABASE_URL
```

### Ошибка подключения к PostgreSQL

```bash
# Проверьте формат DATABASE_URL
echo $DATABASE_URL

# Должен быть: postgresql://user:password@host:port/database
```

### Telegram Bot не отвечает

1. Проверьте токен: `/token` у @BotFather
2. Убедитесь, что бот запущен: `ps aux | grep main.py`
3. Проверьте логи на ошибки

## 📝 Лицензия

MIT License - используйте свободно для своего бизнеса

## 💡 Поддержка

Вопросы? Напишите в Telegram: @your_support_username

## 🎉 Благодарности

Создано для Chocodealers с ❤️ и 🍫
